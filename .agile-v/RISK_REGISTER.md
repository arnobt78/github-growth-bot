# Risk Register

Append-only, cycle-tagged. `RISK-ID | Cycle | Category | Description | Likelihood | Impact | Severity | Mitigation | Owner | Status`

| RISK-ID | Cycle | Category | Description | Likelihood | Impact | Severity | Mitigation | Owner | Status |
|---|---|---|---|---|---|---|---|---|---|
| RISK-0001 | C1 | Technical | All 6 LLM providers rate-limited/down simultaneously (personal free-tier accounts) → Synthesizer produces no recommendations for that day's run. | Medium | Low | Medium | `Synthesizer` degrades gracefully (empty recs + logged error), pipeline still completes with raw metrics (REQ-0005, DEC-0012). No user-facing crash. | Arnob Mahmud | Mitigated |
| RISK-0002 | C1 | Technical | Shared DB session poisoning on a stage's constraint-violation error could crash the entire pipeline run silently. | High (pre-fix) | High | Critical | Fixed via `self.db.rollback()` in `PipelineRunner`'s exception handler; regression test (`test_runner.py`) reproduces the exact scenario. | Arnob Mahmud | Closed — see CAPA-0001 |
| RISK-0003 | C1 | Technical | `@app.on_event` startup/shutdown handlers are deprecated by FastAPI (recommend `lifespan`); no test coverage exists for scheduler start/shutdown since `TestClient` fixtures don't trigger ASGI lifespan events. | Low | Low | Low | Accepted for C1 — functionally correct on pinned `fastapi==0.115.6`, warning-only. Migrate to `lifespan` context manager in a future cycle touching `main.py`. | Arnob Mahmud | Accepted |
| RISK-0004 | C1 | Security | Dashboard backend will be reachable at a public VPS subdomain once deployed; current design relies on a single static API key proxied server-side by Next.js, not full auth. | Low | Medium | Medium | Accepted default per DEC-0006; CORS restricted to the Vercel frontend origin at deploy time. Revisit if threat model changes (e.g. if private-repo data becomes more sensitive than currently scoped). | Arnob Mahmud | Accepted |
| RISK-0005 | C1 | Security | CORS falls back to `["*"]` if `CORS_ORIGINS` env var is unset at deploy time. | Low | Medium | Low | API-key gate is the primary control; document `CORS_ORIGINS` as a required Coolify env var in deploy runbook before going live. | Arnob Mahmud | Open — action before deploy |
| RISK-0006 | C1 | Process | No auto-migration step in the Docker image's `CMD` — `alembic upgrade head` is a manual deploy step. First boot against a fresh Postgres instance will fail without it. | Medium | Medium | Medium | Document as an explicit pre-flight step in the deploy runbook (PLAYBOOK.md). Consider an entrypoint script in a future cycle. | Arnob Mahmud | Open — action before deploy |
| RISK-0007 | C1 | Technical | `GitHubClient.has_file()` treats any non-200 response (rate-limited, transient 5xx) the same as "file confirmed absent" — could produce a false "missing LICENSE" recommendation on a transient API failure. | Low | Low | Low | Plan-mandated behavior (design's own reference code); low-stakes since it only affects one advisory recommendation category, self-corrects next run. Accepted for C1, per Task 4 review. | Arnob Mahmud | Accepted |
| RISK-0008 | C1 | Technical | `LLMRouter`'s and `GitHubClient`'s underlying `httpx.Client` instances are never explicitly closed (no `close()`/context-manager support); one pair leaks per pipeline batch run. | Low | Low | Low | Low impact for a daily single-batch job on a personal VPS; add `close()` support in a future cycle if connection accumulation is ever observed. | Arnob Mahmud | Accepted |
| RISK-0009 | C1 | Process | `BackgroundScheduler` is in-process; if the Docker container is ever run with `uvicorn --workers N > 1`, the daily job would fire N times. | Low | Medium | Low | Dockerfile CMD is single-worker today (`uvicorn app.main:app`, no `--workers` flag). Document as a constraint if scaling is ever considered. | Arnob Mahmud | Accepted, documented |
| RISK-0010 | C1 | Compliance | Auto-starring/forking/following other repos to inflate metrics would violate GitHub's Acceptable Use Policies if ever requested or accidentally introduced. | Low | High | Medium | Hard non-goal (REQ-0000), enforced by code review — no write-capable GitHub API methods exist anywhere in the codebase. Re-verify on any future GitHubClient change. | Arnob Mahmud | Mitigated, standing control |
| RISK-0011 | C1 | Security | A broad `git add -A`/`git add .` followed by a narrower `git add <file>` commits the FULL staged index, not just the most recent target — can silently include unintended files (realized: see CAPA-0003). | Medium (realized once) | High (once realized: public infra exposure) | High (pre-fix), now Low | Standing rule adopted: always `git status`/`git diff --stat` the full index before any commit that follows a broad `add`. Applies to all future git operations on this project. | Arnob Mahmud | Mitigated — process control adopted |
| RISK-0012 | C1 | Security | Moderate advisory GHSA-qx2v-qp2m-jg93 (XSS via unescaped `</style>` in CSS stringification) in `postcss <8.5.10`, bundled in Next.js versions `9.3.4-canary.0 - 16.3.0-canary.5`. Installed Next 16.2.10 and bundled postcss 8.4.31 both in vulnerable ranges. | Low | Low | Low | Project's own top-level `postcss` unaffected; no upstream Next.js patch available yet. Re-check via `npm audit` at Task 20's final dependency-audit gate and at every Next.js patch/minor version bump; revisit if Next.js ships a patched postcss. | Arnob Mahmud | Accepted |
| RISK-0013 | C1 | Security | `pip-audit` (post-frontend-plan audit, 2026-07-21) found 6 distinct starlette CVEs (pinned `starlette==0.41.3` via `fastapi==0.115.6`, fix versions ranging 0.47.2–1.3.1) plus one `pytest==8.3.4` CVE. | Medium (known CVEs, unpatched) | Medium | Medium | Bumped `fastapi` to `0.139.2` (resolves `starlette==1.3.1`, closing all 6 CVEs) and `pytest`/`pytest-asyncio` to `9.1.1`/`1.4.0`. Added `httpx2==2.7.0` (test-only) since starlette 1.3.1's `TestClient` deprecates bare `httpx`; without it a new `StarletteDeprecationWarning` appeared in test output, violating the pristine-test-output convention. Full 42-test suite re-verified green post-bump; `pip-audit` now reports zero known vulnerabilities. | Arnob Mahmud | Resolved |
| RISK-0014 | C1 | Security | `npm audit` (multi-tenant SaaS plan, Task 12, 2026-07-22) found a HIGH severity libvips CVE bundle (CVE-2026-33327/33328/35590/35591, GHSA-f88m-g3jw-g9cj) in `sharp@0.34.5`, `next@16.2.10`'s optional dependency for `next/image` server-side optimization — genuinely reachable, since this project's `SafeImage` component serves optimized GitHub avatars through `next/image` in production. Pre-existing since the original frontend scaffold (Task 3 of the earlier frontend plan); never previously caught or logged. | Medium (reachable, unpatched) | High | High | Fixed via a `package.json` `overrides` entry forcing `sharp` to the patched `0.35.3` (same override mechanism already used for `js-yaml`) — bypasses `next`'s own `^0.34.5` semver floor, which pre-1.0 semver rules would otherwise block. Verified: `npm ls sharp` shows `0.35.3 overridden`; `tsc --noEmit`, `npm run build`, and `npm test` all pass; `npm audit` no longer reports the sharp/libvips finding. | Arnob Mahmud | Resolved |
| RISK-0015 | C1 | Security | `npm audit` (same pass as RISK-0014) also found a moderate path-traversal advisory (GHSA-frvp-7c67-39w9) in `@hono/node-server <2.0.5`, pulled in transitively via `shadcn`'s `@modelcontextprotocol/sdk` dependency. Also pre-existing since the original frontend scaffold, never previously logged. | Low | Low | Low | `shadcn` is a CLI dev-tool used only to scaffold/add UI components at build time — never imported or executed by the running app in production. The vulnerable code path is a Windows-specific backslash-encoding bypass in a static file server this project's Linux/macOS deploy target never invokes. No safe non-breaking fix available (`npm audit fix --force` would bump `shadcn` to a new major, `3.8.3`+, out of scope for this task). Accepted; re-check at the next `shadcn` version bump or Task 17/20-style final dependency-audit gate. | Arnob Mahmud | Accepted |
