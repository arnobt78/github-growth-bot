# Decision Log

Append-only. Never overwritten or reordered. Entries DEC-0001–DEC-0017 are a retroactive reconstruction from session history (Agile-V was adopted mid-project, after C1's backend was already built) — intraday timestamps were not captured contemporaneously, so `TIMESTAMP` reflects the session date only, with sequence preserved by DEC-ID order. All entries from DEC-0018 onward will carry full ISO-8601 timestamps captured live.

---

**DEC-0001** | 2026-07-20 | AGENT: controller (brainstorming) | DECISION: Refused to build any auto-star/auto-fork/engagement-inflation bot; scoped the project to organic-growth tooling only. | RATIONALE: Violates GitHub Acceptable Use Policies; user's request conflated two different asks (fake engagement vs. a legitimate "GitHub Growth Assistant") and the second was clearly the substantive, buildable one. | REQ: REQ-0000

**DEC-0002** | 2026-07-20 | AGENT: controller (brainstorming) | DECISION: Scoped C1 to "Repo health & analytics" only; deferred README suggestions, issue auto-response, release automation, and community/trend discovery to future cycles. | RATIONALE: User's 13-feature ask plus a full multi-agent pipeline was too large for one spec/plan; analytics is the foundation every other feature builds on. | REQ: REQ-0001

**DEC-0003** | 2026-07-20 | AGENT: controller (brainstorming) | DECISION: Custom lightweight `Stage`/`PipelineContext`/`PipelineRunner` pattern chosen over LangGraph. | RATIONALE: The pipeline is linear/deterministic (not an agent dynamically choosing its own next step); LangGraph's graph/branching machinery would be unused complexity. Revisit for a future cycle's agent that needs genuine dynamic decisions (e.g. issue-triage). | REQ: REQ-0001

**DEC-0004** | 2026-07-20 | AGENT: controller (brainstorming) | DECISION: Python/FastAPI backend + Next.js App Router frontend split (not a single-language stack), backend deployed on the user's existing Hetzner VPS via Coolify, frontend on Vercel. | RATIONALE: Matches the user's own established, documented deployment pattern (`docs/SUBDOMAIN_ARNOBMAHMUD_SETUP.md`, `docs/DOCKER_VPS_BACKEND_PLAYBOOK.md`, `docs/VERCEL_PRODUCTION_GUARDRAILS.md`) discovered mid-session in the repo's `docs/` folder, rather than inventing a new generic Docker Compose deployment. | REQ: REQ-0009, REQ-0014

**DEC-0005** | 2026-07-20 | AGENT: controller (brainstorming) | DECISION: LLM fallback order fixed as Groq → Gemini → OpenRouter → Hugging Face → Cloudflare Workers AI → Vercel AI Gateway, ported from the user's existing TypeScript chatbot pattern to Python. | RATIONALE: Reuses a pattern the user already validated in other projects (`docs/LLM_MODEL_IMPLEMENTATION_GUIDE.md`, `docs/LLM_MODEL_SELECTION.md`) rather than designing a new one from scratch; free-tier-first order minimizes cost for a personal tool. | REQ: REQ-0004

**DEC-0006** | 2026-07-20 | AGENT: controller (brainstorming) | DECISION: Dashboard proxies all backend calls through Next.js Route Handlers holding a static API key server-side; browser never sees the key. Chosen over full auth (Clerk/NextAuth) or Vercel password protection. | RATIONALE: Backend will be reachable at a public VPS subdomain; personal single-user tool doesn't need full auth infrastructure, but a bare unauthenticated API would be scanned/hit by bots. Flagged to user as a default, not silently assumed. | REQ: REQ-0013

**DEC-0007** | 2026-07-20 | AGENT: controller (brainstorming) | DECISION: Endpoint paths avoid `analytics`/`analysis`/`tracking`/`performance`/`metrics`; use `insights`/`snapshots`/`benchmarks`/`runs` instead. | RATIONALE: User's explicit instruction — these words commonly trigger ad-blocker/browser-extension URL filters even on a first-party API. | REQ: REQ-0006

**DEC-0008** | 2026-07-20 | AGENT: controller | DECISION: Implementation executed directly on `master` with no git worktree/feature branch, after explicitly asking the user (per `superpowers:using-git-worktrees` consent requirement). | RATIONALE: User chose "work directly on master" for a brand-new solo project with no existing history to protect. | REQ: —

**DEC-0009** | 2026-07-20 | AGENT: task-1-reviewer | DECISION: Added `pyproject.toml` pytest-asyncio config; flagged as Important because test output must be pristine. | RATIONALE: `pytest-asyncio` was a pinned dependency with no config, causing a deprecation warning on every future test run — cheap to fix immediately vs. carrying noise through 10 more tasks. | REQ: — (test infra)

**DEC-0010** | 2026-07-20 | AGENT: task-3-reviewer | DECISION: Reverted an implementer's unauthorized change to `app/db.py` (SQLite `NullPool` config) after empirically proving it was unnecessary — the real fix was fully contained in the in-scope `conftest.py` change. | RATIONALE: File was outside Task 3's scope and already approved in Task 2; per subagent-driven-development discipline, out-of-scope changes to shared production code require justification, and this one didn't hold up under reproduction. | REQ: REQ-0007

**DEC-0011** | 2026-07-20 | AGENT: task-7-reviewer | DECISION: Broadened `LLMRouter`'s retry classification from "429/5xx only" to "any exception" retries the next model within the same provider before moving providers. | RATIONALE: Reviewer proved by reproduction that timeouts and 400-class "model unavailable" responses were skipping straight to the next provider, defeating the purpose of Groq's 3-model fallback list — the exact scenario (Groq deprecating models) the design explicitly anticipated. | REQ: REQ-0004

**DEC-0012** | 2026-07-20 | AGENT: task-8-reviewer | DECISION: `Synthesizer` now degrades to empty recommendations + logged error on any LLM-router exception or non-list JSON response, instead of propagating an uncaught exception/corrupting `ctx.recommendations`. | RATIONALE: Reviewer reproduced an `AttributeError` crash in the downstream `Validator` when the LLM returned syntactically-valid-but-wrong-shape JSON; this directly violated the stage's "must not crash the pipeline" contract. | REQ: REQ-0005

**DEC-0013** | 2026-07-20 | AGENT: task-9-reviewer | DECISION (CRITICAL): Added `self.db.rollback()` as the first action in `PipelineRunner`'s per-stage exception handler. | RATIONALE: Reviewer reproduced a `PendingRollbackError` that would crash the *entire* pipeline run — uncaught — the moment any DB-writing stage (Preprocessor, Assembler) failed with a real constraint violation, because the poisoned shared session broke the runner's own subsequent `StageRun` write. This was the exact failure mode Task 9 existed to prevent. See CAPA-0001. | REQ: REQ-0001

**DEC-0014** | 2026-07-20 | AGENT: task-10-reviewer | DECISION: `run_pipeline_for_all_repos` now constructs `GitHubClient`/`LLMRouter` once per batch instead of once per repo. | RATIONALE: Prevented an unbounded per-repo connection-pool leak that would compound daily once the scheduler (Task 11) went live unattended on the VPS. | REQ: REQ-0008

**DEC-0015** | 2026-07-20 | AGENT: final-whole-branch-reviewer (opus) | DECISION: Assembler extended to persist `BenchmarkRepo`/`Referrer`/`PopularPath` rows (previously computed but never written); `Preprocessor`'s `watchers` field switched from `watchers_count` (a GitHub API alias for star count) to `subscribers_count` (the real value); `SnapshotOut.date` tightened from `object` to `date`; added one true end-to-end pipeline integration test through real production wiring. | RATIONALE: Whole-codebase visibility (not available to per-task reviewers) revealed `GET /repos/{id}/benchmarks` would always return `[]` in production, `watchers` was silently a duplicate metric, and no test had ever executed `app/pipeline/jobs.py::build_stages`'s actual constructor wiring — the exact code path the daily scheduler now depends on. | REQ: REQ-0002, REQ-0003

**DEC-0016** | 2026-07-20 | AGENT: controller | DECISION: Kept all C1 backend work on local `master`; did not push to a remote GitHub repo. | RATIONALE: User chose "leave it local for now" when offered push/PR/keep-local/pause options at branch-completion checkpoint; no remote was configured for this project yet. | REQ: —

**DEC-0017** | 2026-07-20 | AGENT: controller | DECISION: Adopted the Agile-V framework (`.agile-v/`) mid-project, retroactively assigning REQ-IDs to the already-approved design spec and mapping the already-completed backend work to them, rather than re-deriving requirements from scratch. | RATIONALE: User explicitly requested Agile-V governance for the project's continuation (frontend build); retroactive traceability preserves the existing spec/plan as the single source of truth instead of creating a conflicting parallel one. | REQ: all

**DEC-0018** | 2026-07-20 | AGENT: controller | DECISION: Created root `README.md`/`CLAUDE.md` and `docs/PROJECT_WALKTHROUGH.md`/`docs/PROJECT_PLAN.md` at explicit user request, overriding the standing "no unrequested summary docs" preference for these specifically-named files only. Did NOT close Gate 2 (`INT-0001`) despite the user's satisfied tone, since no explicit Approved/Conditional/Rejected decision was given. | RATIONALE: Explicit request supersedes a general preference; Gate approvals require a real decision per `agile-v-compliance` ("Name + role required... Rejected = pipeline halts"), not inference from tone — avoids fabricating an approval record. | REQ: all
